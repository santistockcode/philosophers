repos:
  - repo: local
    hooks:
      - id: norminette
        name: norminette (src/include C files)
        entry: tools/check_norm.sh
        language: script
        files: '^(src|include)/.*\.(c|h)$'
        pass_filenames: true
        stages: [pre-commit]